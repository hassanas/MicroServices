spring.application.name=product-service
server.port=8080
spring.mongodb.uri=mongodb://root:password@127.0.0.1:27018/product-service?authSource=admin
openapi.gateway.docs-access-token=${OPENAPI_GATEWAY_DOCS_ACCESS_TOKEN:local-gateway-docs-token}
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=false
# Swagger UI is served by springdoc at /swagger-ui/index.html.
# OpenAPI JSON is served by springdoc at /v3/api-docs.

# Write application logs inside this service folder.
logging.file.name=logs/product-service.log

# Record every HTTP request handled by embedded Tomcat.
server.tomcat.basedir=.
server.tomcat.accesslog.enabled=true
server.tomcat.accesslog.buffered=false
server.tomcat.accesslog.directory=logs
server.tomcat.accesslog.prefix=access
server.tomcat.accesslog.suffix=.log
server.tomcat.accesslog.pattern=%t %a "%r" %s %b %D
